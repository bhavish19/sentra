# Verifier

## Installation

TEE appraisal tool: sudo apt install -y --no-install-recommends tee-appraisal-tool
Intel SGX application user: sudo apt install libsgx-quote-ex libsgx-dcap-ql

Provide access to Provision Key: sudo usermod -aG sgx_prv